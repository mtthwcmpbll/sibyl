/** Shared frontend test fixtures (mirrors the Rust FIXED_SEED idea: stable inputs). */
export const FIXED_RULES = "bold geometric glyphs, no human faces";
export const FIXED_STYLE = "midnight indigo and gold";
/** Rules containing "boom" trigger a one-time retryable failure in the mock (FR-017). */
export const FAILING_RULES = "boom geometric glyphs";
export const ALL_SUITS = ["cups", "pentacles", "swords", "wands"];
