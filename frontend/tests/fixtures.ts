/** Shared frontend test fixtures (mirrors the Rust FIXED_SEED idea: stable inputs). */
export const FIXED_STYLE = "midnight indigo and gold";
/** A deck style containing "boom" triggers a one-time retryable failure in the mock (FR-017). */
export const FAILING_STYLE = "boom indigo and gold";
export const ALL_SUITS = ["cups", "pentacles", "swords", "wands"];
